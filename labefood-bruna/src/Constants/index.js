export const BASE_URL = "https://us-central1-missao-newton.cloudfunctions.net/fourFoodB"

/* export const validarEmail = email => /[a-zA-Z0-9]+@[a-z]{3}[.a-z]?/.test(email)
export const validarPassword = password => /.{6,}/.test(password)
export const validarName = name => /.{3,}/.test(name)
export const validarCPF = cpf => /^\d{3}\.\d{3}\.\d{3}\-\d{2}$/.test(cpf)
export const validarEndereco = street => /.{4,}/.test(street)
 */
